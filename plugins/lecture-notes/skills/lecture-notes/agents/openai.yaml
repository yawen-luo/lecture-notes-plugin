interface:
  display_name: "Lecture Notes"
  short_description: "Create and refine one annotatable Notion Master Note per lecture"
  default_prompt: "Use ChatGPT's built-in Notion app only. Locate or create the canonical Notion Master Note for this lecture, update that same page from my slides/transcript or comments, and return its link."

policy:
  products:
    - CHAT
    - CODEX
  allow_implicit_invocation: true
